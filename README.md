# 🎓 EduNext

EduNext — это современный учебный проект с серверной частью на **Python**, базой данных **PostgreSQL** и автоматически сгенерированной документацией API с помощью **Swagger UI**.

---

## 🚀 Перед началом работы

Перед настройкой и запуском проекта убедитесь, что у вас установлены следующие компоненты:

| Компонент | Рекомендованная версия | Ссылка для загрузки |
|------------|------------------------|----------------------|
| **PostgreSQL** | 18.0 | [Скачать PostgreSQL](https://www.enterprisedb.com/downloads/postgres-postgresql-downloads) |
| **Python** | 3.13.0 | [Скачать Python](https://www.python.org/downloads/release/python-3130/) |
| **Git** | Последняя версия | [Скачать Git](https://gitforwindows.org/) |

> ⚠️ **Важно:** сервер PostgreSQL должен быть запущен на `localhost:5432`.

---

## 🧰 Настройка окружения

1. **Откройте Visual Studio Code** (или любой другой редактор кода) и создайте папку для проекта.  
2. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/anotheroneee/EduNext.git
   ```
3. **Перейдите в директорию проекта:**
   ```bash
   cd EduNext
   ```
4. **Создайте виртуальную среду:**
   ```bash
   py -3.13 -m venv venv
   ```
5. **Активируйте виртуальное окружение:**
   ```bash
   venv\Scripts\activate
   ```
6. **Обновите pip:**
   ```bash
   python -m pip install --upgrade pip
   ```
7. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```
8. **Добавьте psql в PATH (при необходимости):**
   Перед сетапом нужно убедиться, что psql добавлен в PATH. Для проверки в PowerShell введите: psql --help (если ошибки не возникло переходите к шагу 9). Чтобы добавить в PATH: Win + R -> sysdm.cpl -> Дополнительно -> Переменные среды -> Системные переменные -> Path -> Изменить -> -> Создать -> C:\Program Files\PostgreSQL\18\bin -> Ок -> Перезагрузите систему
9. **Запустите настройку проекта и следуйте инструкциям в терминале:**
   ```bash
   python setup.py
   ```
   После выполнения появится файл `.env`.

10. **Заполните недостающие переменные** в `.env` — значения возьмите из основного документа проекта (EduNext.docx на гугл диске, ссылка на который была отправлена в Telegram).

---

## ▶️ Запуск проекта

После настройки выполните:

```bash
python main.py
```

Откройте в браузере:

👉 [http://localhost:8000/docs](http://localhost:8000/docs)

Вы увидите интерфейс **Swagger**, через который можно тестировать все эндпоинты API.

---

## 🧩 Данные по умолчанию

После первого запуска база данных автоматически заполняется тестовыми данными, включая двух пользователей:

| Роль | Email | Пароль |
|------|--------|--------|
| Пользователь | `user@example.com` | `user` |
| Администратор | `admin@example.com` | `admin` |

---

## 📚 Дополнительная информация

- 📘 **Подробная документация по API:**  
   [API Documentation](https://docs.google.com/spreadsheets/d/1DrUSPuFVCsiwYSc_TW89vgJcziVMBT0t3bNJRTfVRrE/edit?usp=sharing)

- 🗄️ **DB Diagram:**  
   [DB Diagram](https://drive.google.com/file/d/1Nwu5kmpBwS6UsMj4HBThAKPJPpSMTOkj/view?usp=sharing)

---

## 💡 Примечания

- Проект предназначен для локального запуска и тестирования.
- Все зависимости и версии указаны в файле `requirements.txt`.
- В случае проблем с подключением к БД — проверьте настройки `.env` и запущен ли сервер PostgreSQL.
